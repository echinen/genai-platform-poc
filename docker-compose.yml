services:

  api:
    build: .
    ports:
      - "8000:8000"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: genai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres